# wave 2: Gate 2b absolute score-loss bound (gate2b-amendment.md)
name: Safety rerank ablation

on:
  workflow_dispatch:
  push:
    branches:
      - claude/v5-hypothesis-validation-cyna2c
      - experiment/counterfactual-graph
    paths:
      - .github/workflows/safety-rerank-ablation.yml

permissions:
  contents: read

concurrency:
  group: safety-rerank-${{ github.run_id }}
  cancel-in-progress: false

jobs:
  episode:
    name: ${{ matrix.case.case_id }}-${{ matrix.arm }}-r${{ matrix.replicate }}
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        arm: [base, safety_null, safety_rerank]
        replicate: [0, 1, 2]
        case:
          - case_id: b000-k15
            source_case: "000"
            look_ahead: 15
          - case_id: b000-k20
            source_case: "000"
            look_ahead: 20
          - case_id: b000-k40
            source_case: "000"
            look_ahead: 40
          - case_id: b001-k20
            source_case: "001"
            look_ahead: 20
          - case_id: b001-k30
            source_case: "001"
            look_ahead: 30
          - case_id: c000-k1
            source_case: "000"
            look_ahead: 1
          - case_id: c001-k1
            source_case: "001"
            look_ahead: 1
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements.txt
            requirements-simulator.txt
      - run: pip install -r requirements-simulator.txt
      - name: Build development config
        run: >-
          python scripts/build_task_b_config.py
          --source-case "${{ matrix.case.source_case }}"
          --task-id "${{ matrix.case.case_id }}"
          --look-ahead "${{ matrix.case.look_ahead }}"
          --policy-timeout 8
          --output "reports/state-model/gate2/${{ matrix.case.case_id }}.json"
      - name: Run paired-arm episode member
        run: >-
          python scripts/run_risk_ablation.py
          --config "reports/state-model/gate2/${{ matrix.case.case_id }}.json"
          --arm "${{ matrix.arm }}"
          --repeat "${{ matrix.replicate }}"
          --output-dir
          "reports/state-model/gate2/${{ matrix.case.case_id }}-${{ matrix.arm }}-r${{ matrix.replicate }}"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: >-
            safety-rerank-${{ matrix.case.case_id }}-${{ matrix.arm }}-r${{ matrix.replicate }}
          path: >-
            reports/state-model/gate2/${{ matrix.case.case_id }}-${{ matrix.arm }}-r${{ matrix.replicate }}/

  aggregate:
    needs: episode
    if: always() && needs.episode.result == 'success'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/download-artifact@v4
        with:
          pattern: safety-rerank-*
          path: reports/state-model/gate2/downloads
      - name: Combine, summarize, adjudicate
        run: |
          mkdir -p reports/state-model/gate2/aggregate
          find reports/state-model/gate2/downloads -name rows.jsonl \
            -type f -exec cat {} + \
            > reports/state-model/gate2/aggregate/rows.jsonl
          python scripts/run_risk_ablation.py \
            --summarize \
            --output-dir reports/state-model/gate2/aggregate
          python scripts/adjudicate_safety_rerank.py \
            --rows reports/state-model/gate2/aggregate/rows.jsonl \
            --baseline reports/benchmarks/baseline.json \
            --output reports/state-model/gate2/aggregate/adjudication.json \
            --markdown reports/state-model/gate2/aggregate/adjudication.md
          cat reports/state-model/gate2/aggregate/adjudication.md \
            >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        with:
          name: safety-rerank-aggregate
          path: reports/state-model/gate2/aggregate/
