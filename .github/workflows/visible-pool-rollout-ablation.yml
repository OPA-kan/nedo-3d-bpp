name: Visible-pool rollout enforce ablation

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: visible-pool-rollout-${{ github.ref }}
  cancel-in-progress: true

jobs:
  episode:
    name: ${{ matrix.case.case_id }}-${{ matrix.arm }}-r${{ matrix.repeat }}
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        arm: [base, rollout_enforce]
        repeat: [0, 1, 2]
        case:
          - {case_id: b000-k10, source_case: "000", look_ahead: 10}
          - {case_id: b000-k15, source_case: "000", look_ahead: 15}
          - {case_id: b000-k20, source_case: "000", look_ahead: 20}
          - {case_id: b000-k40, source_case: "000", look_ahead: 40}
          - {case_id: b001-k10, source_case: "001", look_ahead: 10}
          - {case_id: b001-k20, source_case: "001", look_ahead: 20}
          - {case_id: b001-k30, source_case: "001", look_ahead: 30}
          - {case_id: b001-k40, source_case: "001", look_ahead: 40}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements-simulator.txt
      - run: pip install -r requirements-simulator.txt
      - name: Build development config
        run: >-
          python scripts/build_task_b_config.py
          --source-case "${{ matrix.case.source_case }}"
          --task-id "${{ matrix.case.case_id }}"
          --look-ahead "${{ matrix.case.look_ahead }}"
          --policy-timeout 8
          --output
          "reports/visible-pool-rollout/${{ matrix.case.case_id }}.json"
      - name: Run episode
        run: >-
          python scripts/run_risk_ablation.py
          --config
          "reports/visible-pool-rollout/${{ matrix.case.case_id }}.json"
          --arm "${{ matrix.arm }}"
          --repeat "${{ matrix.repeat }}"
          --open-final-holdout
          --output-dir
          "reports/visible-pool-rollout/${{ matrix.case.case_id }}-${{ matrix.arm }}-r${{ matrix.repeat }}"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: rollout-${{ matrix.case.case_id }}-${{ matrix.arm }}-r${{ matrix.repeat }}
          path: >-
            reports/visible-pool-rollout/${{ matrix.case.case_id }}-${{ matrix.arm }}-r${{ matrix.repeat }}/

  aggregate:
    needs: episode
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/download-artifact@v4
        with:
          pattern: rollout-*
          path: reports/visible-pool-rollout/downloads
      - name: Combine and summarize
        run: |
          mkdir -p reports/visible-pool-rollout/aggregate
          find reports/visible-pool-rollout/downloads -name rows.jsonl \
            -type f -exec cat {} + \
            > reports/visible-pool-rollout/aggregate/rows.jsonl
          python scripts/run_risk_ablation.py \
            --summarize \
            --output-dir reports/visible-pool-rollout/aggregate
          cat reports/visible-pool-rollout/aggregate/summary.md \
            >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        with:
          name: visible-pool-rollout-aggregate
          path: reports/visible-pool-rollout/aggregate/
      - name: Archive raw output to Drive
        if: always()
        uses: ./.github/actions/archive-to-drive
        with:
          source: |
            reports/visible-pool-rollout/downloads
            reports/visible-pool-rollout/aggregate
          remote: visible-pool-rollout/${{ github.run_id }}
          pointer: >-
            reports/visible-pool-rollout/history/${{ github.run_id }}/drive.json
          credentials: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
          commit: "true"

      - name: Persist compact aggregate history
        env:
          HISTORY_DIR: >-
            reports/visible-pool-rollout/history/${{ github.run_id }}
        run: |
          set -euo pipefail
          mkdir -p "$HISTORY_DIR"
          cp reports/visible-pool-rollout/aggregate/summary.md \
            "$HISTORY_DIR/summary.md"
          cp reports/visible-pool-rollout/aggregate/summary.json \
            "$HISTORY_DIR/summary.json"
          git config user.name "github-actions[bot]"
          git config user.email \
            "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$HISTORY_DIR/summary.md" "$HISTORY_DIR/summary.json"
          git commit \
            -m "Record rollout shadow ${{ github.run_id }} [skip rollout]"
          for attempt in 1 2 3; do
            if git push origin "HEAD:$GITHUB_REF_NAME"; then exit 0; fi
            if [ "$attempt" -eq 3 ]; then exit 1; fi
            git fetch origin "$GITHUB_REF_NAME"
            git rebase "origin/$GITHUB_REF_NAME"
          done
