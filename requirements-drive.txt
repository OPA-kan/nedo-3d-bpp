# Archiving CI raw output to Google Drive (scripts/drive_artifacts.py).
# Only the workflows that call the archive step install this; the agent and
# the simulator do not import it.
google-api-python-client>=2.120,<3
google-auth>=2.28,<3
